const Loading = () => {
  return (
    <main className="z-10 flex h-full w-full items-center justify-center bg-white">
      <progress className="progress w-56"></progress>
    </main>
  );
};

export default Loading;
