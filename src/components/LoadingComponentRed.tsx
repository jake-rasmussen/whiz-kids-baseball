const Loading = () => {
  return (
    <main className="z-8 flex h-full w-full items-center justify-center bg-transparent">
      <progress className="progress progress-primary w-56"></progress>
    </main>
  );
};

export default Loading;
